{
	"name": "ardosia-calculators",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "vite build && rm -rf deploy && mkdir deploy && cp -r plugin.php inc/ dist/ vendor/ README.md deploy/"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^5.0.3",
		"svelte": "5.28.2",
		"vite": "^6.3.3"
	}
}
